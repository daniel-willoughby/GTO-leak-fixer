# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# local preview build
dist-zen/

# solver pipeline working artifacts (regenerated locally)
solver-spike/.street-nodes.staging.json
solver-spike/.accum-flopturn.json
solver-spike/.accum-*.json

# Large sharded postflop corpus: generated locally by solver-spike/promote-shards.mjs
# before a native build. The app loader no-ops without it (bundled slim corpus
# stays the base), so a clone works without these.
public/postflop-shards/
ios/App/App/public/postflop-shards/
ios/App/App/public/postflop-shards-web/
